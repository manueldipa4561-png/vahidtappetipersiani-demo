const state={qty:Number(localStorage.getItem('vahid-demo-qty')||0),price:1700,name:'Kars Sewan anatolico vecchia manifattura cod. D 192'};const $=s=>document.querySelector(s);const money=n=>new Intl.NumberFormat('it-IT',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(n);const cartDrawer=$('#cartDrawer'),backdrop=$('#backdrop'),cartCount=$('#cartCount'),cartEmpty=$('#cartEmpty'),cartContent=$('#cartContent'),cartItems=$('#cartItems'),cartSubtotal=$('#cartSubtotal'),cartTotal=$('#cartTotal'),checkoutModal=$('#checkoutModal'),payButton=$('#payButton');function persist(){localStorage.setItem('vahid-demo-qty',String(state.qty))}function renderCart(){cartCount.textContent=state.qty;const has=state.qty>0;cartEmpty.hidden=has;cartContent.hidden=!has;if(!has){cartItems.innerHTML='';return}const subtotal=state.qty*state.price;cartItems.innerHTML=`<div class="cart-item"><div><h4>${state.name}</h4><small>Cod. D 192 · 190 × 141 cm</small><div class="qty"><button data-action="decrease" aria-label="Diminuisci quantità">−</button><strong>${state.qty}</strong><button data-action="increase" aria-label="Aumenta quantità">+</button></div></div><strong>${money(subtotal)}</strong></div>`;cartSubtotal.textContent=money(subtotal);cartTotal.textContent=money(subtotal);payButton.textContent=`Paga ${money(subtotal)} · Simulazione`}function openCart(){renderCart();cartDrawer.classList.add('open');backdrop.classList.add('show');cartDrawer.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}function closeCart(){cartDrawer.classList.remove('open');backdrop.classList.remove('show');cartDrawer.setAttribute('aria-hidden','true');document.body.style.overflow=''}document.querySelectorAll('.add-to-cart').forEach(btn=>btn.addEventListener('click',()=>{state.qty=Math.max(1,state.qty+1);persist();renderCart();openCart()}));$('#cartTrigger').addEventListener('click',openCart);$('#closeCart').addEventListener('click',closeCart);backdrop.addEventListener('click',closeCart);$('#continueShopping').addEventListener('click',closeCart);cartItems.addEventListener('click',e=>{const action=e.target.dataset.action;if(!action)return;if(action==='increase')state.qty+=1;if(action==='decrease')state.qty=Math.max(0,state.qty-1);persist();renderCart()});$('#checkoutTrigger').addEventListener('click',()=>{closeCart();renderCart();checkoutModal.showModal()});$('#closeCheckout').addEventListener('click',()=>checkoutModal.close());$('#checkoutForm').addEventListener('submit',e=>{e.preventDefault();$('#checkoutForm').parentElement.hidden=true;$('.checkout-summary-pane').hidden=true;$('#successState').hidden=false;state.qty=0;persist();renderCart()});$('#resetDemo').addEventListener('click',()=>{checkoutModal.close();location.reload()});checkoutModal.addEventListener('click',e=>{const r=checkoutModal.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)checkoutModal.close()});$('#cardNumber').addEventListener('input',e=>{let v=e.target.value.replace(/\D/g,'').slice(0,16);e.target.value=v.replace(/(.{4})/g,'$1 ').trim()});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));$('#menuTrigger').addEventListener('click',()=>$('.site-header').classList.toggle('menu-open'));document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>$('.site-header').classList.remove('menu-open')));renderCart();